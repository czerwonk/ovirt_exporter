dist: artifacts
before:
   hooks:
     - go mod download
builds:
  - env:
    - CGO_ENABLED=0
    goos:
    - linux
    - darwin
    - windows
    - freebsd
    goarch:
    - amd64
    - arm
    - arm64
    ldflags: -s -w -X main.version={{.Version}}
    binary: ovirt_exporter 
